import MapView, {
  Marker,
  Polyline,
  PROVIDER_GOOGLE,
  Callout,
  Circle,
} from 'react-native-maps';

export { Marker, Polyline, PROVIDER_GOOGLE, Callout, Circle };

export type {
  MapViewProps,
  MarkerProps,
  PolylineProps,
  CalloutProps,
  CircleProps,
  Region,
} from 'react-native-maps';

export default MapView;
